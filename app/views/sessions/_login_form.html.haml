#login
- if @current_user
  %h4.welcome  Welcome, #{@current_user.name}!
  = link_to ' Log Out', logout_path, :class=>"btn btn-danger glyphicon glyphicon-minus"
- else
  %p.login= link_to ' Log in with your Facebook account', login_path, :class=>"btn btn-primary"
